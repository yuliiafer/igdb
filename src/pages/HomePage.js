import { Feature, HeroHome, Services, Contact} from "../components";

const HomePage = () => {
  return (
    <main>
      <HeroHome />
      <Feature />
      <Services />
      <Contact />
    </main>
  );
};

export default HomePage;
